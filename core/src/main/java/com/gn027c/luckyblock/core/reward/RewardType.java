package com.gn027c.luckyblock.core.reward;

public enum RewardType {
    ITEM,       // Give items
    COMMAND,    // Run console commands
    MESSAGE,    // Send messages/notifications
    EFFECT,     // Potion or particle effects
    STRUCTURE,  // Spawn small structures
    ENTITY,     // Spawn monsters/animals
    TRAP        // Traps (TNT, fall, lava)
}

